#!/usr/bin/python3

def multiply_by_2(a_dictonary):
    new_dictionary = {}
    new_list = list(a_dictionary)
    for i in new_list:
        new_dictionary = dict([i, a_dictionary[i] * 2])
    return new_dictionary

